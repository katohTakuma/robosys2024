#!/usr/bin/python3
# SPDX-FileCopyrightText: 2024 katoh takuma
# SPDX-License-Identifier: BSD-3-Clause

import sys

def process_input(input_text):
    cleaned_text = input_text.strip().replace(" ", "")  # 空白を除いた文字列を確認
    if not cleaned_text.isalpha():  # 英字以外が含まれている場合
        return "変換できません"
    return input_text.strip().upper()  # すべて大文字に変換して返す

if __name__ == "__main__":
    input_text = sys.stdin.read()
    processed_text = process_input(input_text)
    print(processed_text)

