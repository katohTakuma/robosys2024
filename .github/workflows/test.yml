name: test
on: push
jobs:
        test:
                rns-on: ubuntu-latest
                steps:
                        - uses: actions/checkout@v3
                        - name: All test
                          run: bash -xv ./test.bash

